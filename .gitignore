# deps (forge libs are tracked as git submodules — see .gitmodules)
node_modules/
# build
contracts/out/
contracts/cache/
dist/
.turbo/
# env / secrets
.env
.env.*
!.env.example
# misc
.DS_Store
*.log
# browser/visual-loop artifacts
.playwright-mcp/
/*.png
screenshots/
# forge broadcast logs (may contain real tx data / addresses for runs against live chains)
contracts/broadcast/
# Next.js fumadocs generated content cache
apps/site/.source/
