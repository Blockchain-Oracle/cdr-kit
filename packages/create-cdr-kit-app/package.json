{
  "name": "create-cdr-kit-app",
  "version": "0.3.0",
  "description": "Scaffold a runnable cdr-kit starter (mock mode — no wallet/chain needed).",
  "type": "module",
  "bin": {
    "create-cdr-kit-app": "./dist/index.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsdown": "^0.22.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
